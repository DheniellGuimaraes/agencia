jQuery(function($){$('#rsb-recalculate').on('click',function(e){e.preventDefault();const btn=$(this);btn.prop('disabled',true).text('Recalculando...');$.post(RSB.ajaxUrl,{action:'rsb_recalculate',nonce:RSB.nonce},function(resp){alert(resp.success?resp.data.message:(resp.data||'Erro'));location.reload();}).always(function(){btn.prop('disabled',false).text('Recalcular Pontuação e Ranking');});});});
